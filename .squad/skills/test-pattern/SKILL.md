# Test Pattern Skill: Sample Data Builders

Purpose: Provide reusable patterns for building deterministic test data across frontend and backend tests.

Guidelines:
- Use builders to create entities with sensible defaults and fluent overrides.
- Keep builders lightweight and focused; avoid heavy dependencies on DB or external services.

Example (C#): SampleDataBuilder.cs in backend tests

