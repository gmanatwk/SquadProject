import { renderHook, act } from '@testing-library/react-hooks';
// import useUnitConversion from '../../hooks/useUnitConversion';

describe('useUnitConversion hook', () => {
  test('initializes and performs conversion (PENDING)', () => {
    // TODO: renderHook(() => useUnitConversion()) and assert behaviour
  });
});
