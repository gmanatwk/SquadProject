import { describe, it } from '@jest/globals';
// import reducer, { actions } from '../../store/converterSlice';

describe('converterSlice', () => {
  it('should return the initial state (PENDING)', () => {
    // TODO: assert reducer(undefined, { type: '' }) equals initial state
  });

  it.skip('should handle convert action (PENDING)', () => {
    // TODO: dispatch action and assert new state
  });
});
