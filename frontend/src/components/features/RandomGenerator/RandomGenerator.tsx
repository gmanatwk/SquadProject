import React from 'react';

// Skeleton RandomGenerator component
export const RandomGenerator: React.FC = () => {
  return (
    <section>
      <h2>Random Generator (skeleton)</h2>
      {/* TODO: Wire to generator RTK slice and useRandomGenerator hook */}
    </section>
  );
};

export default RandomGenerator;
