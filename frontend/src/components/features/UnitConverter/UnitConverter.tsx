import React from 'react';

// Skeleton UnitConverter component - wiring and hooks will be added later
export const UnitConverter: React.FC = () => {
  return (
    <section>
      <h2>Unit Converter (skeleton)</h2>
      {/* TODO: Add React Hook Form, FormField components, and useUnitConversion hook */}
    </section>
  );
};

export default UnitConverter;
